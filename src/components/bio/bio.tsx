function Bio() {
    return (
        <div>
            <p>
            TODO - turn this bio section into a collection of "tag" styled components.  This won't be a traditional bio, but a collection of tags describing myself.  Allow clicking of tags to support basic react state hooks.  
            </p>
        </div>
    );
}

export default Bio;